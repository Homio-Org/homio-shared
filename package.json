{
  "name": "homio-shared",
  "version": "0.1.0",
  "description": "Central DTOs, MQTT topic helpers, telemetry schema for Homio services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "schemas",
    "openapi"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test:topics": "npm run build && node -e \"const t=require('./dist/mqtt/topics'); const topic=t.telemetryTopic('house-1','dev-1'); if(topic!=='smart-home/house-1/device/dev-1/telemetry'){process.exit(1)} console.log(topic)\""
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
